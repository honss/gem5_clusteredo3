INT BENCHMARKS
python3 queue_gem5_jobs.py \
    --tests libq bzip2 mcf hmmer sjeng omnetpp  \
    --variants none BWifcw RBWifcw \
    --reg-based no \
    --iteration queued_runs \
    --dry-run

FP BENCHMARKS
python3 queue_gem5_jobs.py \
    --tests bwaves gromacs cactusadm gemsfdtd tonto sphinx3 lbm \
    --variants none BWifcw RBWifcw \
    --reg-based no \
    --iteration queued_runs \
    --dry-run

INT CLUSTERED
python3 queue_gem5_jobs.py \
    --tests libq bzip2 mcf hmmer sjeng omnetpp  \
    --variants none BWifcw \
    --reg-based yes \
    --cluster-steer-group-size 32 \
    --intercluster-delay 3 \
    --iteration queued_32 \
    --dry-run

LIBQ delay
python3 queue_gem5_jobs.py \
    --tests libq \
    --variants Wcw \
    --reg-based yes \
    --intercluster-delay 1,2,3,4 \
    --iteration queued_runs \
    --dry-run

python3 queue_gem5_jobs.py \
  --tests libq bzip2 mcf hmmer sjeng omnetpp bwaves gromacs cactusadm tonto sphinx3 lbm \
  --variants RBWifcw \
  --steer ModN:32 \
  --intercluster-delay 1,2 \
  --iteration queued_runs

python3 queue_gem5_jobs.py \
  --tests libq sjeng omnetpp bwaves \
  --variants RBWifcw \
  --steer ModN:32,RegBased:32,PCLowBitHash:1 \
  --cluster-steer-pc-bit 3 \
  --intercluster-delay 2 \
  --iteration queued_runs


FAKE
./build/X86/gem5.opt --outdir=runlogs/temp/123 configs/o3_spec_config.py --libq --intercluster-delay 3